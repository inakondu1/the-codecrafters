## Go else if Statement
It lets the program check multiple conditions one by one until it finds one that is true.

else if is just a way to give the program multiple decisions to choose from, not just yes or a no.
