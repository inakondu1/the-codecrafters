## Go Syntax

### A Go file consists of the following parts:

    1.Package declaration
    2.Import packages
    3.Functions
    4.Statements and expressions
    5. Go Compact Code

## Package Decleration
 
 In every go program they is a package. That is, they is a particular group it must belong to. In a simple way, it is like a container.

 ## Import Package

 The import helps to bring in files into the package. that is bringing in iteams into the container.

 ## Functions
 This has the idea of what is in the package, through this, an instruction can be giving. The function gives instructions on what to print out by using fmt.

 ## Statement and expressions
  
A statement is that thing trhat perform the action. a statemnt can be use to initialize

An expression is anything that produces a value. An expression can be use as a conditional statement

## Go Compact Code

The go compact, using this method entails putting all the codes in a single line.