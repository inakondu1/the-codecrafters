
### Go Maps

A map is a way to store pairs of data: a key and a value.

The key is like a name or label.
The value is the information you want to store.

The length of a map is the number of its elements. You can find it using the len() function.